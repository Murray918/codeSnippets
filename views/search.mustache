<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>search</title>
  </head>
  <body>
    <div class="searchSnippets">
      {{#searchSnippets}}
      <a href="/update/{{_id}}">Title: {{title}}</a>
      <p>Snippet :{{snippet}}</p>
      <p>Notes: {{notes}}</p>
      <p>Language: {{language}}</p>
      <p>Tags: {{tags}}</p>
      <form class="delete" action="/delete/{{id}}" method="post">
        <button type="submit" name="delete">Delete</button>
      </form>
      {{/searchSnippets}}

      {{^searchSnippets}}
      No sippets with that tag name!
      {{/searchSnippets}}
    </div>
  </body>
</html>
